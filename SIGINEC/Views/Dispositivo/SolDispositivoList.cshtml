@model Model.Helpers.PagedData<Model.ViewSolicitudDispositivo>

<table class="table table-hover table-striped">
    <thead>
        <tr>
            <th> Id </th>
            <th> Cliente </th>
            <th> Dispositivo </th>
            <th> Cantidad </th>
            <th> Estado </th>
            <th> Solicita </th>
            <th> Fecha Solicitud </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var sol in Model.Data)
        {
            <tr>
                <td> @Html.DisplayFor(s => sol.IdSolicitud) </td>
                <td> @Html.DisplayFor(s => sol.Cliente) </td>
                <td> @Html.DisplayFor(s => sol.Dispositivo) </td>
                <td> @Html.DisplayFor(s => sol.Cantidad) </td>
                <td> @Html.DisplayFor(s => sol.Estado) </td>
                <td> @Html.DisplayFor(s => sol.Solicita) </td>
                <td> @Html.DisplayFor(s => sol.FSolicitud) </td>
                <td style="text-align:right;">
                    <button type="button" alt="Seguimiento" title="Seguimiento" class="btn btn-warning seguimiento" data-id="@sol.IdSolicitud">
                        <span class="glyphicon glyphicon-sunglasses" aria-hidden="true"></span>
                    </button>
                    <button type="button" alt="Cerrar Solicitud" title="Cerrar Solicitud" class="btn btn-danger cerrar" data-id="@sol.IdSolicitud">
                        <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
                    </button>
                </td>
            </tr>
        }
    </tbody>
    <tfoot>
        <tr>
            <td colspan="8" style="text-align:center;">
                <ul class="pagination">
                    @for (int i = 1; i <= Model.NumberOfPages; i++)
                    {
                        if (i == Model.CurrentPage)
                        {
                            <li class="active">
                                <a class="page-number" href="javascript:void();">@i</a>
                            </li>
                        }
                        else
                        {
                            <li>
                                <a class="page-number" href="javascript:void();">@i</a>
                            </li>
                        }
                    }
                </ul>
            </td>
        </tr>
    </tfoot>
</table>

<script src="~/Assets/js/Logic.js"></script>